---
name: vault-setup
description: Scaffold a new client project vault (plain markdown + git, no Quartz). Pointer stub — the procedure lives in the product-manager skill's vault-setup reference.
allowed-tools: Read, Write, Edit, Bash, Glob, Grep, AskUserQuestion
argument-hint: "[project-name]"
---

> **Invoke with:** `/vault-setup` or `/vault-setup [project-name]`

> **This skill is an invocation target only.** The vault scaffolding procedure was folded into the product-manager skill so setup, structure conventions, and meeting intake live in one place.
>
> **Go read `~/.claude/skills/product-manager/references/vault-setup.md` and follow it.** Load the product-manager skill's other references (`project-structure.md`, `knowledge-graph.md`) for the conventions the new vault must follow.
>
> Note: the old Quartz-based scaffold (static site + GitHub Pages deploy) is retired. New vaults are plain markdown + git. Older vaults still carry unused Quartz files — leave them alone.
