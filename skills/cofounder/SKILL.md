---
name: cofounder
description: George's virtual co-founder. Peer-level strategic thinking partner with persistent memory, decision tracking, and commitment awareness. Loads the cofounder vault, knows what George has committed to, surfaces relevant past thinking, pushes back when George contradicts himself, and maintains memory across sessions.
allowed-tools: Read, Glob, Grep, Write, Edit, Bash, AskUserQuestion, Task
argument-hint: [optional: topic or "" to start fresh]
---

# /cofounder

> **Persona, behaviour, retrieval flow, memory writes, and all vault rules now live in the vault's `CLAUDE.md`.**
>
> This skill exists as an **invocation target only** — typing `/cofounder` signals intent and is recognised by the system, but the actual instructions are in `~/Programming/novosapien/cofounder/CLAUDE.md`, which loads automatically whenever Claude Code operates from inside the cofounder vault.
>
> **If you're reading this file, go read `CLAUDE.md` in the vault root instead.** Everything that was here has been merged there so the cofounder persona is always active in the vault, not just when `/cofounder` is explicitly invoked.
